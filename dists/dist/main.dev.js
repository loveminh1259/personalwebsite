// const vehicles = $(".header-section .vehicle");
// console.log(vehicles.length)
// for (let index = 0; index < vehicles.length; index++) {
//   const element = vehicles[index];
//   const duration = 5 * (index + 1);
//   var tl = new TimelineMax({ repeat: -1, repeatDelay: 0});
//   if (index % 2 == 0) {
//     tl.to(element, duration, { bottom: 0 }).to(element, duration, { top: 0 });
//   } else {
//     tl.to(element, duration, { top: 0 }).to(element, duration, { bottom: 0 });
//   }
// }
// const vehicles1 = $(".about-section .vehicle");
// for (let index = 0; index < vehicles1.length; index++) {
//   const element = vehicles1[index];
//   const duration = 5 * (index + 1);
//   var tl = new TimelineMax({ repeat: -1, repeatDelay: 1 });
//   if (index % 2 == 0) {
//     tl.to(element, duration, { bottom: 0 }).to(element, duration, { top: 0 });
//   } else {
//     tl.to(element, duration, { top: 0 }).to(element, duration, { bottom: 0 });
//   }
// }
"use strict";